<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to 'GYR', root_path, class: "navbar-brand" %>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <% if teacher_signed_in? %>
        <ul class="nav navbar-nav navbar-left">
          <li><%=link_to 'My Students', teacher_students_path(current_teacher)%></li>
          <li><%=link_to "Log Out", destroy_teacher_session_path, method: :delete %></li>
        </ul>
          <ul class="nav navbar-nav navbar-right">
            <p>signed in as <%=current_teacher.full_name%><p>
        </ul>
      <% else %>
        <ul class="nav navbar-nav navbar-right">
          <li><%= link_to 'Sign up', new_teacher_registration_path %></li>
          <li> <%= link_to "Sign In", new_teacher_session_path %> </li>
        </ul>
      <% end %>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
